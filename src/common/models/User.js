export default class User{
    constructor(username, password, role, token, id){
        this.username = username;
        this.password = password;
        this.role = role;
        this.token = token;
        this.id = id;
    }
}