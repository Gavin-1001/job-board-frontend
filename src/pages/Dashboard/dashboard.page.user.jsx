import React from "react";

const UserDashboard = () => {
    //have the jobs displayed here and filters to filter by location, type. Maybe a search bar too to filter jobs

  return (
    <div>
      <h3>User</h3>

    </div>
  );
};

export default UserDashboard;
